package pageObjects;

import org.openqa.selenium.WebDriver;

public class SideBar extends BaseClass {
    public SideBar(WebDriver driver){
        super(driver);
    }
}

//todo: sidebar locators list
